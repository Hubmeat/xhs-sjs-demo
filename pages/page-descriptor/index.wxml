<!--pages/index/index.wxml-->
<view class="section">
  <view>{{ name }}</view><br />
</view>
<view class="nav-list" wx:for="{{pages}}" >
  <navigator url="{{item.path}}" class="navigator">
    {{item.name}}
  </navigator>
</view>
